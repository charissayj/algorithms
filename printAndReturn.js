/*
	Your function will recieve an array with two numbers.
	Print the first value and return the second
*/

function printAndReturn(arr){
	console.log(arr[0]);
	return arr[1];
}

printAndReturn([1,2]);